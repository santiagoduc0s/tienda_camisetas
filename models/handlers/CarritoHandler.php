<?php

require_once './models/handlers/Handler.php';

class CarritoHandler extends Handler{
    // la persistencia del carrito se realizará en sesion
    // no se prevé que en el futuro se persista en una bd
    // pero seria lo aptimo
}
